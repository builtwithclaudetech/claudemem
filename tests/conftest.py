"""Shared pytest fixtures for the claudemem test suite.

Intentionally near-empty at the packaging-skeleton stage. The editable install
(``uv pip install -e``) puts ``claudemem`` on ``sys.path``, so no path
manipulation is needed here.
"""

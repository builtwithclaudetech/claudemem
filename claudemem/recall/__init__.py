"""claudemem.recall — read / rank / serialize, model-free (L2).

Package marker only; no eager submodule imports (tech-design §6.3). This
package must never import ``claudemem.enrich`` or ``anthropic`` (architecture §4).
"""
